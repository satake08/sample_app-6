<h1>投稿一覧</h1>

<!--%= @lists.each do |list| %>-->
<% @lists.each do |list| %><!--@listsの前に＝がない-->

  <p>タイトル</p>
  <!--<span>%= list.title, %></span>↓に変更-->
  <span>
    <%= link_to list.title, "/lists/#{list.id}" %>
  </span>

<% end %>